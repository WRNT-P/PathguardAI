CLAUDE.md
.env
*.json
serviceAccountKey.json
__pycache__/
*.pyc
.venv/
venv/

# Personal AI tooling (teammates don't use Claude / these skills)
.agents/
.claude/

# Documentation / walkthroughs — keep main to runnable code + tests only
docs/

# Test/build artifacts
.pytest_cache/

# Spec / binary docs — keep main to runnable code + tests only
*.pdf

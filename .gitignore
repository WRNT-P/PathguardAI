CLAUDE.md
.env
*.json
serviceAccountKey.json
__pycache__/
*.pyc
.venv/
venv/

# Personal AI tooling (teammates don't use Claude / these skills)
.agents/
.claude/
AGENTS.md

# Documentation / walkthroughs. The .md files ARE tracked as of 2026-08-26:
# ignoring the folder wholesale meant teammates could not read the API contracts
# or the lane plans, which is exactly how the app side spent six days collecting
# a "safe place" with no endpoint to send it to. The 30 MB of PDFs stays out
# (see *.pdf below), and so does anything else that lands in here.
docs/*
!docs/*.md

# Test/build artifacts
.pytest_cache/

# Downloaded GeoLife dataset (Microsoft, ~large, licensed) — not source
backend/data/

# Spec / binary docs — keep main to runnable code + tests only
*.pdf

# Dev notes / instructions — not runnable code, keep local
backend/app/ai/module1_behavior/module1_Description.txt
backend/app/ai/module2_prediction/module2_Description.text
backend/app/ai/module4_search_area/bugs_from_code_review.txt
backend/app/ai/module4_search_area/module4_instruction.txt

# Redundant standalone script (covered by tests/test_module2_detectors.py)
backend/test_wandering_detection.py
